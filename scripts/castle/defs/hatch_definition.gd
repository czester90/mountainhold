@tool
class_name HatchDefinition
extends Resource

## Rectangular opening left in a roof/floor deck so a stair can pass through.
## Dimensions are in metres, expressed in the deck's local X (length) / Z (width).

@export var length: float = 4.6
@export var width: float = 2.6
